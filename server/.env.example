# Copy this file to `.env` and fill in real values before running the server.
# Do NOT commit a `.env` file with secrets to version control.

# Postgres connection string example:
# DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE"

# Example placeholder (replace with your DB credentials):
DATABASE_URL="postgresql://username:password@localhost:5432/my_database"

# Server port (optional)
PORT=3000
