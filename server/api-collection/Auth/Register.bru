meta {
  name: Register
  type: http
  seq: 1
}

post {
  url: {{baseURL}}/auth/register
  body: json
  auth: none
}

body:json {
  {
    "email": "test@sportshis.com",
    "password": "Test123!",
    "fullName": "Test User",
    "role": "CLINICIAN"
  }
}

script:post-response {
  if (res.status === 201 && res.body.data?.token) {
    bru.setEnvVar("authToken", res.body.data.token);
  }
}
