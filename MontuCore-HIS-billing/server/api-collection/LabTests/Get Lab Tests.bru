meta {
  name: Get Lab Tests
  type: http
  seq: 1
}

get {
  url: {{baseURL}}/lab-tests?page=1&limit=10
  body: none
  auth: none
}

params:query {
  page: 1
  limit: 10
  ~athleteId: 
  ~caseId: 
  ~status: 
  ~category: 
}

docs {
  # Get Lab Tests
  
  Retrieves lab tests with optional filters and pagination support.
  
  ## Query Parameters
  - `page`: Page number (default: 1)
  - `limit`: Items per page (default: 10)
  - `athleteId` (optional): Filter by athlete ID
  - `caseId` (optional): Filter by case ID
  - `status` (optional): Filter by status (PENDING, COMPLETED, CANCELLED)
  - `category` (optional): Filter by test category
  
  ## Response
  Returns paginated list of lab tests with:
  - `labTests`: Array of lab test objects
  - `pagination`: Object with page info (page, limit, total, totalPages)
}
  - caseId: Filter by case
  - status: Filter by status (PENDING, COMPLETED, etc.)
  - category: Filter by test category
  - page: Page number (default: 1)
  - limit: Items per page (default: 10)
  - startDate: Filter tests after this date
  - endDate: Filter tests before this date
}
